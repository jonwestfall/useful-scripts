# Archived

This file is archived. It used to track things believed fixed but not
provable in the (Chromium-only, headless) test suite, plus small things
noticed along the way. That job now belongs to GitHub Issues instead — new
bugs, enhancements and testing requests all go there, not here.

Its one live item — verifying the Safari slide-fit fix on a real iPad —
moved to [#27](https://github.com/jonwestfall/useful-scripts/issues/27),
tagged `to test`.

The YouTube embed domain note that was also here did not move: it was
already explained (the original report was a misread control, not this
code), not an open question, so there was nothing left to verify.
